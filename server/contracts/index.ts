/**
 * Re-export the sample backend contracts as a single entry point.
 *
 * This is reference wiring for the template API slice, not a required part of
 * the initial frontend shell.
 */
export * from "./estimate.js";
